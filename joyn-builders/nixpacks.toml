[phases.setup]
nixPkgs = ["python311", "python311Packages.pip"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "python3 migrate.py && python3 seed_catalogue.py && gunicorn app:app --bind 0.0.0.0:$PORT --workers 2 --timeout 120"
