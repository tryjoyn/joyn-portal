[phases.setup]
nixPkgs = ["python311", "python311Packages.pip"]

[phases.install]
cmds = ["pip install --break-system-packages -r requirements.txt"]

[start]
cmd = "python3 -m gunicorn app:app --bind 0.0.0.0:8080 --workers 2 --timeout 120"
